# .env.example - Copy to .env and fill in values

# Client-side safe (public, non-sensitive)
# These are bundled into the client and visible to users
# VITE_API_URL=http://localhost:3000

# Add any other public environment variables here
# Prefix with VITE_ to make them available in the client
